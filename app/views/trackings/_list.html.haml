%h2 Track Listing

%ul.tracking-list
  - trackings.each do |tracking|
    %li.round-shadow.small-round(id="#{ dom_id(tracking)}") 
      %a(href="#{tracking_path(tracking)}")
        %span.big 
          = todays_tracking_results(tracking)
        %span.small
          = tracking.track.unit

      = link_to "delete", tracking_path(tracking), :method => :delete, :remote=>true
.clear
= link_to "Add Tracking", new_tracking_path
